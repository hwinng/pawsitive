enum PetType {
  CAT,
  DOG,
}

enum PetSize {
  SMALL,
  MEDIUM,
  LARGE,
}

enum Status {
  IDLE = 'IDLE',
  PENDING = 'PENDING',
  RESOLVE = 'RESOLVE',
  REJECT = 'REJECT',
}

export { PetType, PetSize, Status }
