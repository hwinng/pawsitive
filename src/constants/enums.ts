enum PetType {
  CAT,
  DOG,
}

enum PetSize {
  SMALL,
  MEDIUM,
  LARGE,
}

enum Status {
  IDLE = 'IDLE',
  PENDING = 'PENDING',
  RESOLVED = 'RESOLVED',
  REJECTED = 'REJECTED',
  RESET = 'RESET',
}

export { PetType, PetSize, Status }
